<UserControl x:Class="CloudStorage.FileEntryControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             PreviewMouseDown="FileEntryControl_PreviewMouseDown_1"
             PreviewTouchDown="FileEntryControl_PreviewTouchDown_1"
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>   
    </UserControl.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="130"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Image Stretch="Uniform" Source="{Binding Image}" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="10,0,0,0" />
        <StackPanel Grid.Column="0" Grid.Row="1">
            <TextBlock Text="{Binding Title}" TextWrapping="Wrap" HorizontalAlignment="Left" Foreground="White" MaxWidth="300" Margin="20,0,0,0"/>
            <!--<TextBlock Text="{Binding IdString}" HorizontalAlignment="Center"/>-->
        </StackPanel>
    </Grid>
</UserControl>
